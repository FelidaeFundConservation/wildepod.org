name: Deploy to Prod / Bhutan

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy"
        required: true
        type: choice
        options:
          - prod
          - bhutan
      run_migrations:
        description: "Run database migrations after deploy"
        required: true
        type: boolean
        default: true
      dryrun:
        description: "Dry run - validate pipeline steps without deploying or migrating"
        required: false
        type: boolean
        default: false

env:
  GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
  DB_PORT: 5440

jobs:
  deploy:
    name: Deploy to ${{ inputs.environment }}${{ inputs.dryrun && ' (dry run)' || '' }}
    runs-on: ubuntu-latest
    env:
      DJANGO_SETTINGS_MODULE: config.settings.ci
      DJANGO_CI_MODE: "true"
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Install sass
        run: npm install -g sass

      - name: Compile SASS
        run: sass --style compressed ./siteapps/static/scss/main.scss:./siteapps/static/css/main.css

      - name: Regenerate requirements.txt
        run: uv export --no-hashes -o requirements.txt

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Collect static files
        run: uv run python manage.py collectstatic --noinput --settings=config.settings.ci

      - name: Deploy to App Engine
        if: ${{ !inputs.dryrun }}
        run: gcloud app deploy ${{ inputs.environment }}.yaml --quiet --project=${{ env.GCP_PROJECT_ID }}

      - name: Dry Run - Skip actual deployment
        if: ${{ inputs.dryrun }}
        run: |
          echo "## Dry Run Summary" >> $GITHUB_STEP_SUMMARY
          echo "DRY RUN enabled - skipping actual deployment." >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Would have run: \`gcloud app deploy ${{ inputs.environment }}.yaml --quiet --project=${{ env.GCP_PROJECT_ID }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "All pre-deployment steps (dependencies, SASS compilation, static files) completed successfully." >> $GITHUB_STEP_SUMMARY

  migrate:
    name: Run Migrations (${{ inputs.environment }})${{ inputs.dryrun && ' (dry run)' || '' }}
    needs: deploy
    if: ${{ inputs.run_migrations }}
    runs-on: ubuntu-latest
    env:
      GOOGLE_CLOUD_PROJECT: ${{ secrets.GCP_PROJECT_ID }}
      USE_CLOUD_SQL_AUTH_PROXY: "True"
      DB_PORT: 5440
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Install dependencies
        run: uv sync

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Resolve DB config for ${{ inputs.environment }}
        id: db-config
        run: |
          if [ "${{ inputs.environment }}" = "prod" ]; then
            echo "instance=${{ env.GCP_PROJECT_ID }}:us-west2:wildepoddb" >> $GITHUB_OUTPUT
            echo "settings=config.settings.prod" >> $GITHUB_OUTPUT
            echo "settings_name=django_settings" >> $GITHUB_OUTPUT
          else
            echo "instance=${{ env.GCP_PROJECT_ID }}:us-west2:wildepoddb" >> $GITHUB_OUTPUT
            echo "settings=config.settings.bhutan" >> $GITHUB_OUTPUT
            echo "settings_name=django_settings" >> $GITHUB_OUTPUT
          fi

      - name: Download Cloud SQL Proxy
        run: |
          curl -o cloud-sql-proxy https://storage.googleapis.com/cloud-sql-connectors/cloud-sql-proxy/v2.14.1/cloud-sql-proxy.linux.amd64
          chmod +x cloud-sql-proxy

      - name: Start Cloud SQL Proxy
        run: |
          ./cloud-sql-proxy --port=${{ env.DB_PORT }} ${{ steps.db-config.outputs.instance }} &
          sleep 3

      - name: Run migrations
        if: ${{ !inputs.dryrun }}
        env:
          SETTINGS_NAME: ${{ steps.db-config.outputs.settings_name }}
        run: |
          uv run python manage.py migrate --settings=${{ steps.db-config.outputs.settings }} 2>&1 | tee migration_output.txt

      - name: Dry Run - Show pending migrations
        if: ${{ inputs.dryrun }}
        env:
          SETTINGS_NAME: ${{ steps.db-config.outputs.settings_name }}
        run: |
          uv run python manage.py migrate --plan --settings=${{ steps.db-config.outputs.settings }} 2>&1 | tee migration_output.txt

      - name: Write migration summary
        if: always()
        run: |
          echo "## ${{ inputs.environment }} Migrations${{ inputs.dryrun && ' (dry run plan)' || '' }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          cat migration_output.txt 2>/dev/null || echo "(no migration output available)" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
