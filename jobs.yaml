runtime: python310
instance_class: B2
service: jobs
entrypoint: gunicorn -b :$PORT jobs:app

manual_scaling:
  instances: 1

handlers:
- url: /static
  static_dir: static/
  secure: always

- url: /.*
  script: auto
  secure: always
  login: admin
